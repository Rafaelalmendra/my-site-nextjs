import styled from "styled-components";

export const Container = styled.div`
  min-height: 10vh;
  display: flex;
  align-items: center;
  justify-content: center;
  bottom: 0;
  span {
    font-size: 1rem;
    font-weight: 300;
  }
  .bi-heart-fill {
    color: var(--blue);
  }
`;
